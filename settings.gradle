plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.8.0'
}

rootProject.name = 'launcher-parent'

include 'launcher', 'launcher-builder', 'launcher-bootstrap', 'launcher-fancy', 'creator-tools'
